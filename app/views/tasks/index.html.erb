<h1>List Tasks</h1>

<ul>
  <% @tasks.each do |task| %>
    <li>
      <h2><%= link_to task.title, task_path(task) %> <%= task.completed %></h2>
      <%= link_to "Edit Task", task_edit_path(task.id) %>


    </li>
  <% end %>
</ul>

<%= link_to "Create Task", tasks_new_path %>

